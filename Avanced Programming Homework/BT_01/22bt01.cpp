#include <bits/stdc++.h>

using namespace std;

int main() {
    double chieucao;
    double cannang;
    cin >> chieucao >> cannang;
    cout << fixed << setprecision(2) << cannang / (chieucao * chieucao);
}
